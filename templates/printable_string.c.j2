/* e2ap_{{name}}.c */
#include "e2ap_{{name}}.h"
#include "rtxsrc/rtxErrCodes.h"
#include "rtxsrc/rtxCharStr.h"
#include "rtxsrc/rtxMemBuf.h"

{% if has_constraint %}
/* Constrained PrintableString */
EXTERN int asn1PE_e2ap_{{name}} (OSCTXT* pctxt, e2ap_{{name}} value)
{
   int stat = 0;
   RTXCTXPUSHTYPENAME (pctxt, "{{name}}");

   PU_SETSIZECONSTRAINT (pctxt, OSUINTCONST({{min_size}}), OSUINTCONST({{max_size}}), OSUINTCONST(0), OSUINT32_MAX);

   char c = (char) rtValidateStr (19, value); 
   if (c != 0) {
      char lbuf[2];
      lbuf[0] = c;
      lbuf[1] = 0;
      rtxErrAddStrParm (pctxt, lbuf);
      return LOG_RTERR (pctxt, RTERR_CONSVIO);
   }

   stat = pe_ConstrainedStringEx (pctxt, value, 0, 8, 7, 7);
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   RTXCTXPOPTYPENAME (pctxt);
   return stat;
}

EXTERN int asn1PD_e2ap_{{name}} (OSCTXT* pctxt, e2ap_{{name}}* ppvalue)
{
   int stat = 0;
   RTXCTXPUSHTYPENAME (pctxt, "{{name}}");

   PU_SETSIZECONSTRAINT (pctxt, OSUINTCONST({{min_size}}), OSUINTCONST({{max_size}}), OSUINTCONST(0), OSUINT32_MAX);

   stat = pd_ConstrainedStringEx (pctxt, ppvalue, 0, 8, 7, 7);
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   RTXCTXPOPTYPENAME (pctxt);
   return stat;
}

{% else %}
/* Unconstrained PrintableString */
EXTERN int asn1PE_e2ap_{{name}} (OSCTXT* pctxt, e2ap_{{name}} value)
{
   int stat = 0;
   RTXCTXPUSHTYPENAME (pctxt, "{{name}}");

   stat = pe_PrintableString (pctxt, value);
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   RTXCTXPOPTYPENAME (pctxt);
   return stat;
}

EXTERN int asn1PD_e2ap_{{name}} (OSCTXT* pctxt, e2ap_{{name}}* ppvalue)
{
   int stat = 0;
   RTXCTXPUSHTYPENAME (pctxt, "{{name}}");

   stat = pd_PrintableString (pctxt, ppvalue);
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   RTXCTXPOPTYPENAME (pctxt);
   return stat;
}

{% endif %}

void asn1Free_e2ap_{{name}} (OSCTXT* pctxt, e2ap_{{name}} pvalue)
{
   if (0 == pvalue) return; 
   rtxMemFreePtr (pctxt, (void*)pvalue);
   pvalue = 0;
}

/* Print to string */
int asn1PrtToStr_e2ap_{{name}} (const char* name, e2ap_{{name}} pvalue, char* buffer, OSSIZE bufSize)
{
   if (rtPrintToStringIndent (buffer, bufSize) < 0) return -1;
   if (rtPrintToStringStr (name, pvalue, buffer, bufSize) < 0) return -1;
   return 0;
}